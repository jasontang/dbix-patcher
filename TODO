key: * todo; + written; - tested
=== stage 1
- schema_loader
- dump schema
- create run
+ add patch
+ run patch
+ find_patch
    + create_date desc
+ is_successful
+ --retry if previous failed
+ move import to run and change patcher script
* clean up
+ --add patches pretending it succeeded
+ use short options ie -d -h -u etc
- change name space of modules to DBIx::Patcher
+ Pg is prerequisite
+ --matchmd5 include md5 lookup of file
+ --link if only one match from md5 lookup, add successful rec
+ --verbose to show cmd run
X --install DBIx::Schema->deploy doesn't deal with namespace
+ --schema dump sql for given type - only Pg supported
* Testing
* Clean up
=== stage 2
* plugin
    * default values
    * collate_files($dir)
=== stage 3
* --type seperate into seperate module
* Mysql optional
* SQLite optional
=== stage 4
